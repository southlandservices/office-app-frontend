const headerContainer = {
  flexGrow: 1,
  minHeight: '65px'
};

const pageHeaderLeft = {
  float: 'left',
  marginTop: 10
};

const pageTitle = {
  fontSize: '30px',
  fontWeight: 500
};

const pageOptions = {
  float: 'right',
  minHeight: '65px',
  marginTop: '10px'
};

const styles = Object.assign({}, {
  headerContainer,
  pageHeaderLeft,
  pageTitle,
  pageOptions
});

export default styles;