const styles = theme => ({
    margin: {
        margin: theme.spacing.unit * 2,
    },
    paper: {
        padding: theme.spacing.unit,
        margin: '0 auto',
        maxWidth: 600
    },
});
export default styles;