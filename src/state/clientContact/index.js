import clientContactReducer from './reducers';

export { default as clientContactActions } from './actions';
export { default as clientContactOperations } from './operations';
export { default as clientContactTypes } from './types';

export default clientContactReducer;
